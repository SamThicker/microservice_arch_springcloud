pipeline {

    agent any

    stages {

        stage("拉取代码"){
           steps{
                script {
                    echo "拉取代码"
                }
           }
        }

        stage("项目构建"){
           steps{
                script {
                    echo "项目构建"
                }
           }
        }

        stage("发布更新"){
           steps{
                script {
                    echo "发布更新"
                }
           }
        }
    }
}